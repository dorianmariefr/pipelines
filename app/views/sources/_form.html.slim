.source-js.card
  = f.hidden_field :_destroy

  .p
    = f.label :kind, t(".kind")
    = f.select :kind, source_kinds_options(f.object)

  .p(data-sources-target="remove")
    = button_to t(".remove"), "#", method: :get, class: "button--link text-red-600", data: { action: "sources#remove" }
